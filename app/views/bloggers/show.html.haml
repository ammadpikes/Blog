- provide(:title, "#{@blogs.title}")
.generic-container
  .container
    .row
      .col-lg-12
        %h1= "Title: #{@blogs.title}"
    .row
      .col-lg-12
        %p= "#{@blogs.content}"
    .row
      .col-lg-12 
        = link_to 'Edit',  edit_blogger_path(@blogs), class: 'btn btn-primary'

        =link_to 'Show All Blogs', bloggers_path, class: 'btn btn-warning'